[package]
name = "minimal-ssr-hydrate"
version = "0.1.0"
edition = "2021"
publish = false
description = "Minimal Orbital SSR + hydrate shell teaching host"

[lints]
workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
orbital = { package = "orbital-ui", path = "../../orbital", version = "0.1.4", default-features = false }
leptos = { workspace = true }
leptos_meta = { workspace = true }
leptos_router = { workspace = true }
cfg-if = { workspace = true }

axum = { workspace = true, optional = true }
leptos_axum = { workspace = true, optional = true }
tokio = { workspace = true, optional = true }
tower-http = { workspace = true, optional = true }
simple_logger = { workspace = true, optional = true }
log = { workspace = true, optional = true }
any_spawner = { version = "0.3", features = ["futures-executor"], optional = true }

console_error_panic_hook = { workspace = true, optional = true }
console_log = { workspace = true, optional = true }
wasm-bindgen = { workspace = true, optional = true }

[features]
default = []
hydrate = [
    "leptos/hydrate",
    "orbital/hydrate",
    "dep:console_error_panic_hook",
    "dep:console_log",
    "dep:wasm-bindgen",
    "dep:log",
]
ssr = [
    "leptos/ssr",
    "leptos_meta/ssr",
    "leptos_router/ssr",
    "orbital/ssr",
    "dep:axum",
    "dep:leptos_axum",
    "dep:tokio",
    "dep:tower-http",
    "dep:simple_logger",
    "dep:log",
    "dep:any_spawner",
]

[[bin]]
name = "minimal-ssr-hydrate"
path = "src/main.rs"
required-features = ["ssr"]
